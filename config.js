// Supabase configuration.
// Replace the two values below with your project's public URL and anon key.
// These are safe to use in a browser; NEVER put a Supabase service_role key here.
window.SUPABASE_URL = "YOUR_SUPABASE_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
